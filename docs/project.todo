✔ Complete the deterministic DF-cell viewport work in `docs/viewport.todo`, including the mount option, 128 by 64 default, and `ds.viewport(width, height)` command. @done(26-07-21 04:57)
✔ Update `ds.get(...)` to accept and parse hierarchy paths for nested components. @done(26-07-21 04:57)
☐ Add new `ds.screenshot(...)` method to allow taking screenshots of mounted components under test.
☐ Update `ds.get(...)` to accept and parse simple CSS-like selector queries.
☐ Create a list of possible useful commands that can be added for non-ui (behavioral) game logic testing, such as `ds.get_state(...)`, `ds.set_state(...)`, `ds.spy(...)` (for DF game events), `ds.trigger_event(...)`, etc.
