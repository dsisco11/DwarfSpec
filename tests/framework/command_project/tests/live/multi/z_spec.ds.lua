-- Last deterministic multiple-selection live proof.

describe('ordered multiple selection Z', function()
    it('runs last by canonical identity', function()
        assert.equals(1, ds.protocol_version)
    end)
end)
