-- First deterministic multiple-selection live proof.

describe('ordered multiple selection A', function()
    it('runs first by canonical identity', function()
        assert.equals(1, ds.protocol_version)
    end)
end)
